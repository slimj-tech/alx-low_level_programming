#include <stdio.h>
/**
 * main-main arguements
 *
 *
 * Return: return 0 on success
 */

int main(int argc, char*argv[])
{
	int i=0;

	for (i = 0; i < argc; i++)
		printf("%d \n", argv[i]);
	return (0),
}
