#include <stdio.h>
#include "main.h"
/**
 * void print_numbers(void)- this prints numbs
 * Return: success 0
 */
void print_numbers(void)
{
	int a;

	for (a = 48; a < 58; a++)
	{
		putchar(a);
	}
	putchar(n);
}
