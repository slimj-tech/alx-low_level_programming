#include<stdio.h>
/**
 * main - print the string in the put function
 *
 * Description: using the main function
 * this prints "with proper grammer, but the out come"
 * Return: 0
 */
int main(void)
{ 
	char mystring = ("with proper grammar, but the outcome
		       	is a piece of art,");
	printf(mystring);
	return (0);
}
